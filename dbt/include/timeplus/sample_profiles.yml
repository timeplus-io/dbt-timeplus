default:
  outputs:

    dev:
      type: timeplus
      schema: [database name]
      host: [host]

      port: [port]
      user: [user]
      password: [password]

    prod:
      type: timeplus
      schema: [database name]
      host: [host]

      port: [port]
      user: [user]

  target: dev
